---
- name: Converge
  hosts: all
  tasks:
    - name: "Include wandansible.prometheus_exporter.exabgp"
      ansible.builtin.include_role:
        name: "wandansible.prometheus_exporter.exabgp"
      vars:
        exabgp_exporter_github_token: "{{ lookup('env', 'GITHUB_TOKEN') }}"
